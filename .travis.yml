# repo branches

branches:
  only:
  - master

# job environment

sudo: false

language: ruby

rvm:
  - 2.6.3
  - 2.5.5

# job phases

install:
  - bundler install --with=development

script:
  - bundler exec rake test
